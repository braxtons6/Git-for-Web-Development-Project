blank face lp

https://codepen.io/braxtons6/pen/YzNYybX

    1. What is Semantic HTML? 
	- HTML that introduces meaning to the page instead of just presentation
    2. What is HTML used for? 
	- Defining the structure of your content
    3. What is an attribute and where do we put it? 
	-Attributes provide additional information about HTML elements. in the start tag.
    4. What is the h1 tag used for? How many times should I use it on a page?
	- h1 tags are used to define the most important heading. Only one per page.
    5. Name two tags that have required attributes
	-<img> and <a>
    6. What do we put in the head of our HTML document? 
	<!DOCTYPE html>
    7. What is an id? 
	- an id is used to specifiy a unique id for an HTML element
    8. What elements can I add an id to? 
	- you can add an id to any element
    9. How many times can I use the same id on a page? 
	- Once
    10. What is a class? 
	- allows you to set a single style for multiple elements
    11. What elements can I add a class to? 
	-any elements
    12. How many times can I use the same class on a page? 
	-Multiple times	
    13. How do I get my link to open in a new tab?
	-<p>this link will open in a new tab <a href="https://www.espn.com/" target="_blank">Espn</a>.</p>
    14. What is the alt attribute in the image tag used for? 
	- It provides alternative information for an user should they not be able to see the screen or are using a screen reader
    15. How do I reference an id?
	- #id
    16. What is the difference between a section and a div
	- section means that the content inside this element is grouped and relate to a single theme, div only represents its child elements and doesn't
	have special meaning
    17. What is CSS used for? 
	-styling a webpage
    18. How to we select an element? Example - every h2 on the page
	- h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
	-the id selector uses the id attribute of an html element to select a specific element, the class selector selects elements with a specific class attribute.
	you would use id on something that appears on the page only once, ie header,footer. you would use class for elements that appear once or more than once ie
	paragraphs, links, buttons
    20. How do we select classes in CSS?
	- .class
    21. How do we select a p element with a single class of “human””?
	- p.human {}
    22. What is a parent child selector? When would this be useful? 
	- the parent child selector selects all elements that are a direct child of the specified element. it is useful when you want to change certain parts of the
	html while keeping other parts the same or doing soemthing else with them
    23. How do you select all links within a div with the class of sidebar?
	-.sidebar a {}
    24. What is a pseudo selector?
	-a selector that selects elements that are in a specific state, ie :hover
    25. What do we use the change the spacing between lines?
	-line-spacing:
    26. What do we use to change the spacing between letters?
	-letter-spacing: 1em;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
	- text-transform: uppercase, text-transform: lowercase, text-transform: capitalize
    28. How do I add a 1px border around my div that is dotted and black?
	- { border:1px dotted; border-color:black }
    29. How do I select everything on the page?
	- *{}
    30. How do I write a comment in CSS?
	- /* insert comment here */
    31. How do I find out what file I am in, when I am using the command line? 
	-pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
	- ls 
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
	- rm <file>, you have to be careful because you might delete something you dont want to, no warning
    34. Why should I use version control? 
	-It allows you to compare files, identify differences, and merge changes if needed.
    35. How often should I commit to github?
	- As often as possible, or after you've made some progress
    36. What is the command we would use to push our repo up to github? 
	-git push
    37. Walk me through Lambda's git flow.
	-fork the repo, clone it with the https, create a branch (git checkout -b braxton-sislo), use git status to know what you are changing, 
         use git add to add files, Git commit -m(always make a useful message), git push

